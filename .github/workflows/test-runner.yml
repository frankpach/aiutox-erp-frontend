# Este workflow ya no es necesario.
# El flujo de CI/CD se maneja con:
#   - GitHub Actions (ubuntu-latest) para validación de código
#   - scripts/50-deploy/build-and-deploy.ps1 para build Docker y deploy local