name: deprecated-test-runner

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op placeholder
        run: |
          echo "Este workflow quedó obsoleto.\n"
          echo "Validaciones actuales viven en workflows activos y scripts/50-deploy/build-and-deploy.ps1."